#!/bin/bash
realpath=`readlink -fn $0`
export GIT_EXEC_PATH=`dirname $realpath`/../libexec/git-core GIT_TEMPLATE_DIR=`dirname $realpath`/../share/git-core/templates
exec -a `dirname $realpath`/git `dirname $realpath`/git.real "$@"
